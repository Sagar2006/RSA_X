# RSA-X Kaggle Configuration Override (T4 GPU Optimized)

dataset:
  num_samples: 1000             # Full test split representation
  max_seq_len: 512               # standard attention sequence length
  batch_size: 4                 # Optimized batch size for T4 GPU VRAM

model:
  device: "cuda"                # Force GPU acceleration

storage:
  save_raw_samples: 10          # Keep a representable subset of raw logs
